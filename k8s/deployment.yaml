apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-deployment
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: contador
  template:
    metadata:
      labels:
        app: contador
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      containers:
        - name: default-container
          image: ghcr.io/EmilioamvS/contador:8336811
          ports:
            - containerPort: 8080
