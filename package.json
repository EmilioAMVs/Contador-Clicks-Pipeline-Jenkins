{
  "name": "my-demo-site",
  "type": "module",
  "version": "1.0.0",
  "description": "Demo para probar pipeline creativo con Jenkins y GitHub Pages",
  "scripts": {
    "lint": "eslint . --ext .js",
    "test": "set NODE_OPTIONS=--experimental-vm-modules && jest --coverage",
    "build": "mkdir -p dist && cp index.html style.css script.js dist/",
    "report": "node generate-report.js && echo 'Reporte generado en reports/summary.txt'"
  },
  "devDependencies": {
    "@babel/core": "^7.28.4",
    "@babel/eslint-parser": "^7.28.4",
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  }
}
